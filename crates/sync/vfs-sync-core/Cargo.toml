[package]
name = "vfs-sync-core"
version = "0.3.0"
edition = "2021"
description = "Common types for VFS S3 synchronization"
license = "MIT"
repository = "https://github.com/ternbusty/monaka-fs"

[dependencies]
