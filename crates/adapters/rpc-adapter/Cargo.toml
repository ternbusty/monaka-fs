[package]
name = "rpc-adapter"
version = "0.3.1"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
vfs-rpc-protocol = { path = "../../rpc/vfs-rpc-protocol" }
wit-bindgen = { version = "0.61.1", default-features = false, features = ["realloc", "macros", "bitflags"] }
prost = "0.14"
log = "0.4"

[build-dependencies]
wit-bindgen = "0.61.1"
