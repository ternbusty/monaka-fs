[workspace]
members = [
  "fs-core",
  "fs-wasm",
  "vfs-provider",
  "examples/rust",
]
exclude = [
  ".tmp",  # Temporary work-in-progress code
]
resolver="3"

# examples/c is built with its own Makefile (no cargo)

[profile.dev]
opt-level = 1
