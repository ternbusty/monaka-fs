[package]
name = "bench-08-app"
version = "0.1.0"
edition = "2021"

# Standalone package, not part of workspace
[workspace]

[[bin]]
name = "bench-08-app"
path = "src/main.rs"

[dependencies]

[profile.release]
opt-level = "z"
lto = true
