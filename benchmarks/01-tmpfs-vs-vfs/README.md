# Benchmark 01 (tmpfs vs VFS vs ext4)

Measures the overhead of Halycon VFS abstraction layer compared to host tmpfs mount.

## Run

```bash
make run-usecase-benchmark-01
```

Or manually

```bash
cd benchmarks/01-tmpfs-vs-vfs
./run-bench.sh
```
