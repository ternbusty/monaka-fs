# Benchmark 03 (Local VFS vs RPC)

Measures the overhead of RPC communication compared to local in-memory VFS.

## Running

```bash
make run-usecase-benchmark-03
```

Or manually

```bash
cd benchmarks/03-local-vs-rpc
./run-bench.sh
```
