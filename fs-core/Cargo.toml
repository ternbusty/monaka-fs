[package]
name = "fs-core"
version = "0.1.0"
edition = "2024"
resolver="3"

[dependencies]
log = { version = "0.4", optional = true, default-features = false }

[features]
default = ["std"]
std = []
logging = ["dep:log"]
