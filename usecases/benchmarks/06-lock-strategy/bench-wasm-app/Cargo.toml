[package]
name = "bench-lock-wasm-app"
version = "0.1.0"
edition = "2021"

# Standalone package, not part of workspace
[workspace]

[[bin]]
name = "bench-lock-wasm-app"
path = "src/main.rs"

[profile.release]
opt-level = 3
lto = true
