[package]
name = "host-concurrent-runner"
version = "0.1.0"
edition = "2021"

# Standalone package, not part of workspace
[workspace]

[dependencies]
# WASM runtime
wasmtime = { version = "44.0.1", features = ["component-model"] }
wasmtime-wasi = "44.0.1"

# VFS host implementation
vfs-host = { path = "../../../../crates/hosts/vfs-host" }

# Utilities
anyhow = "1.0"

[profile.release]
opt-level = 3
